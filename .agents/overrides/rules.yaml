overrides:
  - id: packaging.versioned_artifacts
    title: Versioned release artifact filenames
    status: active
    summary: Release builds must include the app version in distributable filenames, for example WinStart_v3.0.1.exe and WinStart_Setup_v3.0.1.exe.
