# 项目接手

## 目的

让新的 AI 会话或新的协作者快速对齐 `WinStart` 的定位、规则和当前开发状态。

## 执行步骤

1. 先阅读 `START_HERE.md`。
2. 再按 `START_HERE.md` 规定的顺序读取本地文件。
3. 先输出项目定位、规则红线、当前状态和下一步建议, 再开始改动。
4. 如果需求涉及 UI、启动逻辑或配置结构, 先判断落点是在 `src/ui/`, `src/core/`, 还是文档层。
