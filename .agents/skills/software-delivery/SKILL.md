# 软件交付

## 目的

帮助 `WinStart` 在保持轻量的前提下, 让代码、文档和交付方式继续可维护。

## 执行步骤

1. 新功能先判断属于运行时逻辑、界面层还是打包安装层。
2. 配置结构变化时同步检查 `src/core/config_manager.py` 和相关 UI。
3. 用户可见行为变化时同步更新说明文档或 release notes。
4. 发布前优先在 Windows 环境验证启动、图标和安装相关行为。
